import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  bottomText: {
    textAlign: 'center',
    fontSize: 8,
    width: 70,
    marginTop: 5,
  },
});
